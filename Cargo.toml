[package]
name = "pytest-fast-discovery"
version = "0.1.0"
edition = "2021"

[lib]
name = "pytest_fast_discovery"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.27", features = ["extension-module"] }
